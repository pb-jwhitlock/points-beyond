---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Points Beyond - AI Automation Consultant">
  <div class="ctrl-page">
    <section class="hero">
      <div class="hero-wrapper">
        <div class="hero-content">
          <h1>
            <span class="hero-line">Your content.</span>
            <span class="hero-line">AI's <span class="highlight">answer.</span></span>
          </h1>
          <p class="hero-sub">
            Strategic content optimization for businesses ready to lead in the AI search era. We position your brand as the source AI trusts.
          </p>
          <div class="hero-cta">
            <a href="#form" class="btn btn-primary">
              <span>Get Your Free Proposal</span>
              <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
            </a>
            <a href="/points-beyond/services/" class="btn btn-secondary">Learn More</a>
          </div>
        </div>
        <div class="hero-badge">
          <span class="badge-dot"></span>
          <span>AI Automation Consultant</span>
        </div>
      </div>
    </section>

    <section class="intro">
      <div class="container">
        <div class="intro-grid">
          <div class="intro-label">
            <span class="label-line"></span>
            <span>The Shift</span>
          </div>
          <div class="intro-content">
            <h2>The way people find you has fundamentally changed.</h2>
            <p>
              AI-powered search engines, voice assistants, and answer engines are reshaping discovery. ChatGPT, Perplexity, and Google AI Overview are the new gatekeepers. Points Beyond helps you become the answer they deliver.
            </p>
          </div>
        </div>
      </div>
    </section>

    <section class="services">
      <div class="container">
        <div class="section-header">
          <h2>What we do</h2>
        </div>
        <div class="services-grid">
          <div class="service-card">
            <div class="card-icon">
              <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/><path d="M11 8v6"/><path d="M8 11h6"/></svg>
            </div>
            <h3>Answer Engine Optimization</h3>
            <p>Position your content to be the definitive answer when AI systems respond to user queries. Be the source that gets cited.</p>
            <div class="card-tags">
              <span>ChatGPT</span>
              <span>Perplexity</span>
              <span>Claude</span>
            </div>
          </div>
          <div class="service-card">
            <div class="card-icon">
              <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg>
            </div>
            <h3>Search Engine Optimization</h3>
            <p>Proven techniques combined with AI-era strategies. Rank in both conventional results and AI-curated responses.</p>
            <div class="card-tags">
              <span>Google</span>
              <span>Local SEO</span>
              <span>Technical</span>
            </div>
          </div>
          <div class="service-card service-card-wide">
            <div class="card-icon">
              <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/></svg>
            </div>
            <div class="card-body">
              <h3>AI Voice Receptionist</h3>
              <p>Never miss a call. Our AI receptionist answers 24/7 with natural conversation—booking appointments, answering FAQs, and ensuring every caller gets immediate attention.</p>
            </div>
            <div class="card-features">
              <div class="feature-item">
                <span class="feature-check">✓</span>
                <span>24/7 Availability</span>
              </div>
              <div class="feature-item">
                <span class="feature-check">✓</span>
                <span>Auto Scheduling</span>
              </div>
              <div class="feature-item">
                <span class="feature-check">✓</span>
                <span>FAQ Handling</span>
              </div>
              <div class="feature-item">
                <span class="feature-check">✓</span>
                <span>Zero Wait Time</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section class="form-section" id="form">
      <div class="container">
        <div class="form-wrapper">
          <div class="form-content">
            <h2>Ready to get started?</h2>
            <p>Get your free proposal and see how we can position your business for the AI search era.</p>
          </div>
          <div class="form-card">
            <form action="#" method="POST">
              <div class="form-row">
                <div class="form-group">
                  <input type="text" name="name" placeholder="Your Name" required />
                </div>
                <div class="form-group">
                  <input type="email" name="email" placeholder="Email Address" required />
                </div>
              </div>
              <div class="form-row">
                <div class="form-group">
                  <input type="url" name="website" placeholder="Website URL" />
                </div>
                <div class="form-group">
                  <input type="tel" name="phone" placeholder="Phone Number" />
                </div>
              </div>
              <button type="submit" class="btn btn-primary btn-full">
                <span>Get Your Free Proposal</span>
                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
              </button>
            </form>
          </div>
        </div>
      </div>
    </section>

    <footer class="site-footer">
      <div class="container">
        <div class="footer-content">
          <div class="footer-brand">Points Beyond</div>
          <div class="footer-links">
            <a href="/points-beyond/services/">Services</a>
            <a href="/points-beyond/about/">About</a>
            <a href="/points-beyond/contact/">Contact</a>
          </div>
        </div>
      </div>
    </footer>
  </div>
</Layout>

<style>
  @import url('https://fonts.googleapis.com/css2?family=Syne:wght@400;500;600;700;800&display=swap');

  .ctrl-page {
    --color-bg: #0a0a0a;
    --color-surface: #141414;
    --color-border: #262626;
    --color-text: #ffffff;
    --color-text-dim: #888888;
    --color-accent: #05c92f;
    --color-accent-hover: #04b529;

    background: var(--color-bg);
    color: var(--color-text);
    font-family: 'Syne', sans-serif;
    font-weight: 400;
    min-height: 100vh;
    font-size: 18px;
    line-height: 1.6;
  }

  .container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 3rem;
  }

  /* Hero */
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding: 4rem 0;
    position: relative;
  }

  .hero-wrapper {
    width: 100%;
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 3rem;
  }

  .hero-content {
    max-width: 1000px;
  }

  .hero h1 {
    font-size: clamp(3.5rem, 12vw, 9rem);
    font-weight: 700;
    line-height: 1;
    margin-bottom: 2.5rem;
    letter-spacing: -0.03em;
  }

  .hero-line {
    display: block;
  }

  .highlight {
    color: var(--color-accent);
  }

  .hero-sub {
    font-size: clamp(1.25rem, 2.5vw, 1.75rem);
    color: var(--color-text-dim);
    max-width: 650px;
    margin-bottom: 3rem;
    line-height: 1.5;
    font-weight: 400;
  }

  .hero-cta {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .hero-badge {
    position: absolute;
    top: 3rem;
    right: 3rem;
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.75rem 1.5rem;
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 100px;
    font-size: 0.9rem;
    color: var(--color-text-dim);
  }

  .badge-dot {
    width: 8px;
    height: 8px;
    background: var(--color-accent);
    border-radius: 50%;
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
  }

  /* Buttons */
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    padding: 1.25rem 2.5rem;
    border-radius: 100px;
    font-size: 1.1rem;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s cubic-bezier(0.165, 0.84, 0.44, 1);
    cursor: pointer;
    border: none;
  }

  .btn-primary {
    background: var(--color-accent);
    color: #000;
  }

  .btn-primary:hover {
    background: var(--color-accent-hover);
    transform: scale(1.02);
  }

  .btn-secondary {
    background: transparent;
    color: var(--color-text);
    border: 1px solid var(--color-border);
  }

  .btn-secondary:hover {
    background: var(--color-surface);
    border-color: var(--color-text-dim);
  }

  .btn-full {
    width: 100%;
  }

  /* Intro */
  .intro {
    padding: 8rem 0;
    border-top: 1px solid var(--color-border);
  }

  .intro-grid {
    display: grid;
    grid-template-columns: 250px 1fr;
    gap: 4rem;
    align-items: start;
  }

  .intro-label {
    display: flex;
    align-items: center;
    gap: 1rem;
    font-size: 1rem;
    color: var(--color-text-dim);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    font-weight: 500;
  }

  .label-line {
    width: 40px;
    height: 1px;
    background: var(--color-accent);
  }

  .intro-content h2 {
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: 600;
    margin-bottom: 1.5rem;
    line-height: 1.15;
    letter-spacing: -0.02em;
  }

  .intro-content p {
    font-size: 1.25rem;
    color: var(--color-text-dim);
    line-height: 1.7;
    max-width: 700px;
  }

  /* Services */
  .services {
    padding: 6rem 0 8rem;
  }

  .section-header {
    margin-bottom: 4rem;
  }

  .section-header h2 {
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 600;
    letter-spacing: -0.02em;
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
  }

  .service-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 24px;
    padding: 2.5rem;
    transition: all 0.3s cubic-bezier(0.165, 0.84, 0.44, 1);
  }

  .service-card:hover {
    border-color: var(--color-accent);
    transform: translateY(-4px);
  }

  .service-card-wide {
    grid-column: 1 / -1;
    display: grid;
    grid-template-columns: auto 1fr auto;
    gap: 2.5rem;
    align-items: center;
  }

  .card-icon {
    width: 64px;
    height: 64px;
    background: rgba(5, 201, 47, 0.1);
    border-radius: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-accent);
    margin-bottom: 1.5rem;
  }

  .service-card-wide .card-icon {
    margin-bottom: 0;
  }

  .service-card h3 {
    font-size: 1.5rem;
    font-weight: 600;
    margin-bottom: 1rem;
    letter-spacing: -0.01em;
  }

  .service-card p {
    color: var(--color-text-dim);
    font-size: 1.1rem;
    line-height: 1.6;
    margin-bottom: 1.5rem;
  }

  .service-card-wide .card-body p {
    margin-bottom: 0;
  }

  .card-tags {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
  }

  .card-tags span {
    padding: 0.5rem 1rem;
    background: var(--color-bg);
    border: 1px solid var(--color-border);
    border-radius: 100px;
    font-size: 0.85rem;
    color: var(--color-text-dim);
  }

  .card-features {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 0.75rem;
  }

  .feature-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 1rem;
    color: var(--color-text);
  }

  .feature-check {
    color: var(--color-accent);
    font-weight: 700;
  }

  /* Form Section */
  .form-section {
    padding: 8rem 0;
    background: var(--color-surface);
    border-top: 1px solid var(--color-border);
  }

  .form-wrapper {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .form-content h2 {
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 600;
    margin-bottom: 1rem;
    letter-spacing: -0.02em;
  }

  .form-content p {
    font-size: 1.25rem;
    color: var(--color-text-dim);
    line-height: 1.6;
  }

  .form-card {
    background: var(--color-bg);
    border: 1px solid var(--color-border);
    border-radius: 24px;
    padding: 2.5rem;
  }

  .form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
    margin-bottom: 1rem;
  }

  .form-group input {
    width: 100%;
    padding: 1.25rem 1.5rem;
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 12px;
    font-family: 'Syne', sans-serif;
    font-size: 1rem;
    color: var(--color-text);
    transition: border-color 0.2s ease;
  }

  .form-group input::placeholder {
    color: var(--color-text-dim);
  }

  .form-group input:focus {
    outline: none;
    border-color: var(--color-accent);
  }

  .form-card .btn {
    margin-top: 0.5rem;
  }

  /* Footer */
  .site-footer {
    padding: 3rem 0;
    border-top: 1px solid var(--color-border);
  }

  .footer-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .footer-brand {
    font-size: 1.25rem;
    font-weight: 600;
  }

  .footer-links {
    display: flex;
    gap: 2rem;
  }

  .footer-links a {
    color: var(--color-text-dim);
    text-decoration: none;
    font-size: 1rem;
    transition: color 0.2s ease;
  }

  .footer-links a:hover {
    color: var(--color-text);
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .intro-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .services-grid {
      grid-template-columns: 1fr;
    }

    .service-card-wide {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .card-features {
      grid-template-columns: repeat(4, 1fr);
    }

    .form-wrapper {
      grid-template-columns: 1fr;
      gap: 3rem;
    }
  }

  @media (max-width: 768px) {
    .container {
      padding: 0 1.5rem;
    }

    .hero-wrapper {
      padding: 0 1.5rem;
    }

    .hero-badge {
      position: static;
      margin-bottom: 2rem;
      display: inline-flex;
    }

    .hero-cta {
      flex-direction: column;
    }

    .btn {
      width: 100%;
      justify-content: center;
    }

    .intro, .services, .form-section {
      padding: 5rem 0;
    }

    .form-row {
      grid-template-columns: 1fr;
    }

    .card-features {
      grid-template-columns: 1fr 1fr;
    }

    .footer-content {
      flex-direction: column;
      gap: 1.5rem;
      text-align: center;
    }
  }

  @media (max-width: 480px) {
    .ctrl-page {
      font-size: 16px;
    }

    .hero h1 {
      font-size: 2.75rem;
    }

    .card-features {
      grid-template-columns: 1fr;
    }
  }
</style>
