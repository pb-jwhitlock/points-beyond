---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Points Beyond - AI Automation Consultant">
  <div class="tech-page">
    <div class="grid-bg"></div>

    <section class="hero">
      <div class="hero-wrapper">
        <div class="hero-content">
          <div class="hero-meta">
            <span class="status-dot"></span>
            <span>AI Automation Consultant</span>
          </div>
          <h1>Points<br/>Beyond</h1>
          <p class="subtitle">Strategic content optimization for businesses ready to dominate AI-powered search.</p>
          <div class="hero-stats">
            <div class="stat">
              <span class="stat-value">24/7</span>
              <span class="stat-label">AI Coverage</span>
            </div>
            <div class="stat-divider"></div>
            <div class="stat">
              <span class="stat-value">AEO+SEO</span>
              <span class="stat-label">Dual Strategy</span>
            </div>
          </div>
        </div>
        <div class="hero-form">
          <div class="form-card">
            <div class="form-header">
              <span class="form-tag">[ START ]</span>
              <h3>Get Your Free Proposal</h3>
            </div>
            <form action="#" method="POST">
              <div class="form-group">
                <label>Name</label>
                <input type="text" name="name" required />
              </div>
              <div class="form-group">
                <label>Email</label>
                <input type="email" name="email" required />
              </div>
              <div class="form-group">
                <label>Website</label>
                <input type="url" name="website" />
              </div>
              <div class="form-group">
                <label>Phone</label>
                <input type="tel" name="phone" />
              </div>
              <button type="submit" class="form-submit">
                <span>Initialize</span>
                <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
              </button>
            </form>
          </div>
        </div>
      </div>
    </section>

    <section class="intro">
      <div class="container">
        <div class="intro-label">// CONTEXT</div>
        <h2>The discovery layer has evolved.</h2>
        <p>
          AI systems now mediate how audiences find your brand. ChatGPT, Perplexity, Claude, and Google AI Overview are the new gatekeepers. Points Beyond engineers content strategies that position you as the authoritative answer.
        </p>
      </div>
    </section>

    <section class="services">
      <div class="container">
        <div class="services-header">
          <span class="section-tag">// CAPABILITIES</span>
          <h2>Core Services</h2>
        </div>
        <div class="services-grid">
          <div class="service-card">
            <div class="card-number">01</div>
            <div class="card-content">
              <h3>Answer Engine Optimization</h3>
              <p>Position your content as the definitive source AI systems cite when responding to user queries. Structure content for direct, authoritative responses.</p>
              <div class="card-tags">
                <span>ChatGPT</span>
                <span>Perplexity</span>
                <span>Claude</span>
              </div>
            </div>
          </div>
          <div class="service-card">
            <div class="card-number">02</div>
            <div class="card-content">
              <h3>Search Engine Optimization</h3>
              <p>Hybrid approach combining proven optimization techniques with AI-era strategies. Rank in both conventional results and AI-curated responses.</p>
              <div class="card-tags">
                <span>Google</span>
                <span>Bing</span>
                <span>AI Overview</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section class="voice-section">
      <div class="container">
        <div class="voice-card">
          <div class="voice-header">
            <div class="voice-badge">
              <span class="pulse"></span>
              <span>ALWAYS ON</span>
            </div>
            <div class="card-number">03</div>
          </div>
          <h2>AI Voice Receptionist</h2>
          <p class="voice-intro">
            Autonomous call handling. 24/7 availability. Zero latency response. Natural language processing that adapts to each caller's needs.
          </p>
          <div class="voice-features">
            <div class="feature">
              <div class="feature-icon">
                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
              </div>
              <span>Appointment Scheduling</span>
            </div>
            <div class="feature">
              <div class="feature-icon">
                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3"/><line x1="12" y1="17" x2="12.01" y2="17"/></svg>
              </div>
              <span>FAQ Resolution</span>
            </div>
            <div class="feature">
              <div class="feature-icon">
                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>
              </div>
              <span>Natural Dialog</span>
            </div>
            <div class="feature">
              <div class="feature-icon">
                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
              </div>
              <span>Zero Wait Time</span>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section class="cta-section">
      <div class="container">
        <div class="cta-content">
          <span class="cta-tag">// NEXT STEP</span>
          <h2>Ready to optimize for AI?</h2>
          <p>Let's discuss your strategy.</p>
          <a href="/contact/" class="cta-button">
            <span>Contact Us</span>
            <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
          </a>
        </div>
      </div>
    </section>
  </div>
</Layout>

<style>
  @import url('https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;600&family=Space+Grotesk:wght@400;500;600;700&display=swap');

  .tech-page {
    --color-bg: #0a0a0f;
    --color-surface: #12121a;
    --color-border: #1e1e2d;
    --color-text: #e4e4eb;
    --color-text-dim: #6b6b7a;
    --color-accent: #00ff88;
    --color-accent-dim: rgba(0, 255, 136, 0.15);
    --color-cyan: #00d4ff;
    --color-purple: #a855f7;

    background: var(--color-bg);
    color: var(--color-text);
    font-family: 'Space Grotesk', sans-serif;
    min-height: 100vh;
    position: relative;
  }

  .grid-bg {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-image:
      linear-gradient(rgba(30, 30, 45, 0.4) 1px, transparent 1px),
      linear-gradient(90deg, rgba(30, 30, 45, 0.4) 1px, transparent 1px);
    background-size: 60px 60px;
    pointer-events: none;
    z-index: 0;
  }

  /* Hero */
  .hero {
    padding: 6rem 2rem;
    position: relative;
    z-index: 1;
  }

  .hero::before {
    content: '';
    position: absolute;
    top: -50%;
    left: 50%;
    transform: translateX(-50%);
    width: 800px;
    height: 800px;
    background: radial-gradient(circle, rgba(0, 255, 136, 0.08) 0%, transparent 60%);
    pointer-events: none;
  }

  .hero-wrapper {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 400px;
    gap: 4rem;
    align-items: center;
  }

  .hero-meta {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 2rem;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.8rem;
    color: var(--color-accent);
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  .status-dot {
    width: 8px;
    height: 8px;
    background: var(--color-accent);
    border-radius: 50%;
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; box-shadow: 0 0 0 0 rgba(0, 255, 136, 0.4); }
    50% { opacity: 0.8; box-shadow: 0 0 0 8px rgba(0, 255, 136, 0); }
  }

  .hero h1 {
    font-size: 5.5rem;
    font-weight: 700;
    line-height: 0.95;
    margin-bottom: 1.5rem;
    letter-spacing: -0.03em;
  }

  .subtitle {
    font-size: 1.15rem;
    color: var(--color-text-dim);
    line-height: 1.7;
    max-width: 450px;
    margin-bottom: 2.5rem;
  }

  .hero-stats {
    display: flex;
    align-items: center;
    gap: 2rem;
  }

  .stat {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .stat-value {
    font-family: 'JetBrains Mono', monospace;
    font-size: 1.5rem;
    font-weight: 600;
    color: var(--color-accent);
  }

  .stat-label {
    font-size: 0.75rem;
    color: var(--color-text-dim);
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  .stat-divider {
    width: 1px;
    height: 40px;
    background: var(--color-border);
  }

  /* Form */
  .form-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    padding: 2rem;
  }

  .form-header {
    margin-bottom: 1.5rem;
  }

  .form-tag {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.7rem;
    color: var(--color-accent);
    letter-spacing: 0.1em;
    display: block;
    margin-bottom: 0.5rem;
  }

  .form-card h3 {
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--color-text);
  }

  .form-group {
    margin-bottom: 1rem;
  }

  .form-group label {
    display: block;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.7rem;
    color: var(--color-text-dim);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 0.5rem;
  }

  .form-group input {
    width: 100%;
    padding: 0.875rem 1rem;
    border: 1px solid var(--color-border);
    background: var(--color-bg);
    font-family: 'Space Grotesk', sans-serif;
    font-size: 1rem;
    color: var(--color-text);
    transition: border-color 0.2s ease;
  }

  .form-group input:focus {
    outline: none;
    border-color: var(--color-accent);
  }

  .form-submit {
    width: 100%;
    padding: 1rem;
    background: var(--color-accent);
    color: var(--color-bg);
    border: none;
    font-family: 'Space Grotesk', sans-serif;
    font-size: 0.9rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    transition: background 0.2s ease, transform 0.2s ease;
    margin-top: 0.5rem;
  }

  .form-submit:hover {
    background: var(--color-cyan);
    transform: translateY(-2px);
  }

  /* Intro */
  .container {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 2rem;
    position: relative;
    z-index: 1;
  }

  .intro {
    padding: 6rem 2rem;
    border-top: 1px solid var(--color-border);
  }

  .intro-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.75rem;
    color: var(--color-accent);
    text-transform: uppercase;
    letter-spacing: 0.15em;
    margin-bottom: 1.5rem;
  }

  .intro h2 {
    font-size: 2.5rem;
    font-weight: 600;
    margin-bottom: 1.5rem;
    max-width: 600px;
  }

  .intro p {
    color: var(--color-text-dim);
    line-height: 1.9;
    font-size: 1.1rem;
    max-width: 700px;
  }

  /* Services */
  .services {
    padding: 4rem 2rem 6rem;
  }

  .services-header {
    display: flex;
    align-items: baseline;
    gap: 2rem;
    margin-bottom: 3rem;
  }

  .section-tag {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.75rem;
    color: var(--color-accent);
    text-transform: uppercase;
    letter-spacing: 0.15em;
  }

  .services-header h2 {
    font-size: 1.75rem;
    font-weight: 600;
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
  }

  .service-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    padding: 2rem;
    display: flex;
    gap: 1.5rem;
    transition: border-color 0.2s ease;
  }

  .service-card:hover {
    border-color: var(--color-accent);
  }

  .card-number {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.8rem;
    color: var(--color-accent);
    flex-shrink: 0;
  }

  .card-content h3 {
    font-size: 1.25rem;
    font-weight: 600;
    margin-bottom: 0.75rem;
  }

  .card-content p {
    color: var(--color-text-dim);
    line-height: 1.7;
    font-size: 0.95rem;
    margin-bottom: 1.25rem;
  }

  .card-tags {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
  }

  .card-tags span {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.65rem;
    color: var(--color-accent);
    background: var(--color-accent-dim);
    padding: 0.3rem 0.6rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  /* Voice Section */
  .voice-section {
    padding: 4rem 2rem 6rem;
    border-top: 1px solid var(--color-border);
  }

  .voice-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    padding: 2.5rem;
    max-width: 800px;
  }

  .voice-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1.5rem;
  }

  .voice-badge {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.7rem;
    color: var(--color-accent);
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  .pulse {
    width: 6px;
    height: 6px;
    background: var(--color-accent);
    border-radius: 50%;
    animation: pulse 2s infinite;
  }

  .voice-card h2 {
    font-size: 1.75rem;
    font-weight: 600;
    margin-bottom: 1rem;
  }

  .voice-intro {
    color: var(--color-text-dim);
    line-height: 1.8;
    font-size: 1.05rem;
    margin-bottom: 2rem;
    max-width: 550px;
  }

  .voice-features {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1rem;
  }

  .feature {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.75rem 1rem;
    background: var(--color-bg);
    border: 1px solid var(--color-border);
  }

  .feature-icon {
    color: var(--color-accent);
  }

  .feature span {
    font-size: 0.9rem;
    color: var(--color-text);
  }

  /* CTA */
  .cta-section {
    padding: 6rem 2rem;
    border-top: 1px solid var(--color-border);
  }

  .cta-content {
    text-align: center;
    max-width: 500px;
    margin: 0 auto;
  }

  .cta-tag {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.75rem;
    color: var(--color-accent);
    text-transform: uppercase;
    letter-spacing: 0.15em;
    display: block;
    margin-bottom: 1.5rem;
  }

  .cta-content h2 {
    font-size: 2.25rem;
    font-weight: 600;
    margin-bottom: 0.75rem;
  }

  .cta-content p {
    color: var(--color-text-dim);
    margin-bottom: 2rem;
    font-size: 1.1rem;
  }

  .cta-button {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 1rem 2rem;
    background: transparent;
    color: var(--color-accent);
    border: 1px solid var(--color-accent);
    font-size: 0.9rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    text-decoration: none;
    transition: all 0.2s ease;
  }

  .cta-button:hover {
    background: var(--color-accent);
    color: var(--color-bg);
  }

  /* Responsive */
  @media (max-width: 900px) {
    .hero-wrapper {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .hero h1 {
      font-size: 4rem;
    }

    .hero-form {
      max-width: 450px;
    }

    .services-grid {
      grid-template-columns: 1fr;
    }

    .voice-features {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 600px) {
    .hero {
      padding: 4rem 1.5rem;
    }

    .hero h1 {
      font-size: 2.75rem;
    }

    .hero-stats {
      flex-direction: column;
      align-items: flex-start;
      gap: 1rem;
    }

    .stat-divider {
      display: none;
    }

    .intro, .services, .voice-section, .cta-section {
      padding: 4rem 1.5rem;
    }

    .intro h2 {
      font-size: 1.75rem;
    }

    .voice-card {
      padding: 1.5rem;
    }

    .cta-content h2 {
      font-size: 1.75rem;
    }
  }
</style>
