---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Points Beyond - AI Automation Consultant">
  <div class="warm-page">
    <section class="hero">
      <div class="hero-wrapper">
        <div class="hero-content">
          <div class="hero-badge">AI Automation Consultant</div>
          <h1>Points Beyond</h1>
          <p class="subtitle">Helping businesses create content that connects with both AI systems and the humans they serve.</p>
        </div>
        <div class="hero-form">
          <div class="form-card">
            <div class="form-icon">
              <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M14 9a2 2 0 0 1-2 2H6l-4 4V4c0-1.1.9-2 2-2h8a2 2 0 0 1 2 2v5Z"/><path d="M18 9h2a2 2 0 0 1 2 2v11l-4-4h-6a2 2 0 0 1-2-2v-1"/></svg>
            </div>
            <h3>Let's Start a Conversation</h3>
            <p class="form-intro">Get your free proposal and see how we can help.</p>
            <form action="#" method="POST">
              <div class="form-group">
                <input type="text" name="name" placeholder="Your Name" required />
              </div>
              <div class="form-group">
                <input type="email" name="email" placeholder="Email Address" required />
              </div>
              <div class="form-group">
                <input type="url" name="website" placeholder="Website URL" />
              </div>
              <div class="form-group">
                <input type="tel" name="phone" placeholder="Phone Number" />
              </div>
              <button type="submit" class="form-submit">Get Started</button>
            </form>
          </div>
        </div>
      </div>
      <div class="hero-wave">
        <svg viewBox="0 0 1440 120" fill="none" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="none">
          <path d="M0 120L60 105C120 90 240 60 360 45C480 30 600 30 720 37.5C840 45 960 60 1080 67.5C1200 75 1320 75 1380 75L1440 75V120H1380C1320 120 1200 120 1080 120C960 120 840 120 720 120C600 120 480 120 360 120C240 120 120 120 60 120H0Z" fill="currentColor"/>
        </svg>
      </div>
    </section>

    <section class="intro">
      <div class="container">
        <div class="intro-icon">
          <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4"/><path d="M12 8h.01"/></svg>
        </div>
        <h2>The Way People Find You Is Changing</h2>
        <p>
          AI-powered search engines, voice assistants, and answer engines are reshaping how your audience discovers your brand. We help you create content strategies that feel authentic while making sure you're visible wherever your customers are looking.
        </p>
      </div>
    </section>

    <section class="services">
      <div class="container">
        <h2>How We Help</h2>
        <div class="services-grid">
          <div class="service-card">
            <div class="service-icon">
              <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/><path d="M11 8v6"/><path d="M8 11h6"/></svg>
            </div>
            <h3>Answer Engine Optimization</h3>
            <p>When someone asks ChatGPT or Perplexity about your industry, you want to be the answer they get. We make that happen.</p>
          </div>
          <div class="service-card">
            <div class="service-icon">
              <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg>
            </div>
            <h3>Search Engine Optimization</h3>
            <p>Traditional search still matters. We blend proven techniques with AI-era strategies so you show up everywhere.</p>
          </div>
        </div>
      </div>
    </section>

    <section class="voice-section">
      <div class="container">
        <div class="voice-card">
          <div class="voice-icon">
            <svg xmlns="http://www.w3.org/2000/svg" width="36" height="36" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/></svg>
          </div>
          <div class="voice-content">
            <h2>AI Voice Receptionist</h2>
            <p class="voice-intro">
              Your customers deserve immediate attention, even at 2 AM. Our AI receptionist answers every call with natural conversation—booking appointments and answering questions while you focus on what you do best.
            </p>
            <ul class="voice-features">
              <li>Book, reschedule, and confirm appointments automatically</li>
              <li>Answer questions about hours, services, and pricing</li>
              <li>Handle complex requests with natural conversation</li>
              <li>Available 24/7 with zero hold time</li>
            </ul>
          </div>
        </div>
      </div>
    </section>

    <section class="cta-section">
      <div class="container">
        <div class="cta-content">
          <h2>Ready to Be Found?</h2>
          <p>Let's talk about how we can help your business thrive in the AI era.</p>
          <a href="/points-beyond/contact/" class="cta-button">Get in Touch</a>
        </div>
      </div>
    </section>
  </div>
</Layout>

<style>
  @import url('https://fonts.googleapis.com/css2?family=Nunito:wght@400;500;600;700&family=Fraunces:opsz,wght@9..144,400;9..144,500;9..144,600&display=swap');

  .warm-page {
    --color-cream: #fef7ed;
    --color-peach: #fce8d5;
    --color-terracotta: #c97b5d;
    --color-terracotta-dark: #a85d42;
    --color-sage: #7d9a7d;
    --color-charcoal: #3d3835;
    --color-warm-gray: #6b6360;
    --color-white: #ffffff;

    background: var(--color-cream);
    color: var(--color-charcoal);
    font-family: 'Nunito', sans-serif;
    font-weight: 400;
    min-height: 100vh;
  }

  /* Hero */
  .hero {
    background: linear-gradient(180deg, var(--color-peach) 0%, var(--color-cream) 100%);
    padding: 6rem 2rem 0;
    position: relative;
  }

  .hero-wrapper {
    max-width: 1100px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 380px;
    gap: 4rem;
    align-items: center;
    padding-bottom: 4rem;
  }

  .hero-badge {
    display: inline-block;
    background: var(--color-terracotta);
    color: var(--color-white);
    padding: 0.5rem 1.25rem;
    border-radius: 50px;
    font-size: 0.85rem;
    font-weight: 600;
    margin-bottom: 1.5rem;
  }

  .hero h1 {
    font-family: 'Fraunces', serif;
    font-size: 4rem;
    font-weight: 500;
    line-height: 1.1;
    margin-bottom: 1.5rem;
    color: var(--color-charcoal);
  }

  .subtitle {
    font-size: 1.2rem;
    color: var(--color-warm-gray);
    line-height: 1.7;
    max-width: 480px;
  }

  .hero-wave {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    color: var(--color-cream);
    line-height: 0;
  }

  .hero-wave svg {
    width: 100%;
    height: 60px;
  }

  /* Form */
  .form-card {
    background: var(--color-white);
    border-radius: 24px;
    padding: 2.5rem;
    box-shadow: 0 20px 60px rgba(201, 123, 93, 0.15);
    text-align: center;
  }

  .form-icon {
    color: var(--color-terracotta);
    margin-bottom: 1rem;
  }

  .form-card h3 {
    font-family: 'Fraunces', serif;
    font-size: 1.5rem;
    font-weight: 500;
    margin-bottom: 0.5rem;
    color: var(--color-charcoal);
  }

  .form-intro {
    color: var(--color-warm-gray);
    font-size: 0.95rem;
    margin-bottom: 1.5rem;
  }

  .form-group {
    margin-bottom: 0.875rem;
  }

  .form-group input {
    width: 100%;
    padding: 1rem 1.25rem;
    border: 2px solid var(--color-peach);
    border-radius: 12px;
    background: var(--color-cream);
    font-family: 'Nunito', sans-serif;
    font-size: 1rem;
    color: var(--color-charcoal);
    transition: border-color 0.3s ease, background 0.3s ease;
  }

  .form-group input::placeholder {
    color: var(--color-warm-gray);
  }

  .form-group input:focus {
    outline: none;
    border-color: var(--color-terracotta);
    background: var(--color-white);
  }

  .form-submit {
    width: 100%;
    padding: 1rem;
    background: var(--color-terracotta);
    color: var(--color-white);
    border: none;
    border-radius: 12px;
    font-family: 'Nunito', sans-serif;
    font-size: 1rem;
    font-weight: 700;
    cursor: pointer;
    transition: background 0.3s ease, transform 0.2s ease;
    margin-top: 0.5rem;
  }

  .form-submit:hover {
    background: var(--color-terracotta-dark);
    transform: translateY(-2px);
  }

  /* Intro */
  .container {
    max-width: 1000px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .intro {
    padding: 6rem 2rem;
    text-align: center;
  }

  .intro-icon {
    color: var(--color-sage);
    margin-bottom: 1.5rem;
  }

  .intro h2 {
    font-family: 'Fraunces', serif;
    font-size: 2.5rem;
    font-weight: 500;
    margin-bottom: 1.5rem;
    color: var(--color-charcoal);
  }

  .intro p {
    color: var(--color-warm-gray);
    line-height: 1.9;
    font-size: 1.15rem;
    max-width: 700px;
    margin: 0 auto;
  }

  /* Services */
  .services {
    padding: 4rem 2rem 6rem;
    background: var(--color-white);
  }

  .services h2 {
    font-family: 'Fraunces', serif;
    font-size: 2rem;
    font-weight: 500;
    text-align: center;
    margin-bottom: 3rem;
    color: var(--color-charcoal);
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
    max-width: 800px;
    margin: 0 auto;
  }

  .service-card {
    background: var(--color-cream);
    border-radius: 20px;
    padding: 2rem;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .service-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 15px 40px rgba(201, 123, 93, 0.12);
  }

  .service-icon {
    width: 56px;
    height: 56px;
    background: var(--color-peach);
    border-radius: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-terracotta);
    margin-bottom: 1.25rem;
  }

  .service-card h3 {
    font-family: 'Fraunces', serif;
    font-size: 1.35rem;
    font-weight: 500;
    margin-bottom: 0.75rem;
    color: var(--color-charcoal);
  }

  .service-card p {
    color: var(--color-warm-gray);
    line-height: 1.7;
    font-size: 1rem;
  }

  /* Voice Section */
  .voice-section {
    padding: 6rem 2rem;
    background: var(--color-cream);
  }

  .voice-card {
    background: var(--color-white);
    border-radius: 24px;
    padding: 3rem;
    display: flex;
    gap: 2.5rem;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.06);
  }

  .voice-icon {
    width: 72px;
    height: 72px;
    background: linear-gradient(135deg, var(--color-sage) 0%, #5d7a5d 100%);
    border-radius: 20px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-white);
    flex-shrink: 0;
  }

  .voice-content h2 {
    font-family: 'Fraunces', serif;
    font-size: 1.75rem;
    font-weight: 500;
    margin-bottom: 1rem;
    color: var(--color-charcoal);
  }

  .voice-intro {
    color: var(--color-warm-gray);
    line-height: 1.8;
    font-size: 1.05rem;
    margin-bottom: 1.5rem;
  }

  .voice-features {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    gap: 0.75rem;
  }

  .voice-features li {
    color: var(--color-charcoal);
    padding-left: 1.75rem;
    position: relative;
    font-size: 0.95rem;
  }

  .voice-features li::before {
    content: "✓";
    position: absolute;
    left: 0;
    color: var(--color-sage);
    font-weight: 700;
  }

  /* CTA */
  .cta-section {
    padding: 6rem 2rem;
    background: linear-gradient(180deg, var(--color-cream) 0%, var(--color-peach) 100%);
    text-align: center;
  }

  .cta-content h2 {
    font-family: 'Fraunces', serif;
    font-size: 2.5rem;
    font-weight: 500;
    margin-bottom: 1rem;
    color: var(--color-charcoal);
  }

  .cta-content p {
    color: var(--color-warm-gray);
    margin-bottom: 2rem;
    font-size: 1.15rem;
  }

  .cta-button {
    display: inline-block;
    padding: 1rem 2.5rem;
    background: var(--color-charcoal);
    color: var(--color-white);
    font-size: 1rem;
    font-weight: 700;
    border-radius: 50px;
    text-decoration: none;
    transition: background 0.3s ease, transform 0.2s ease;
  }

  .cta-button:hover {
    background: var(--color-terracotta);
    transform: translateY(-2px);
    color: var(--color-white);
  }

  /* Responsive */
  @media (max-width: 900px) {
    .hero-wrapper {
      grid-template-columns: 1fr;
      gap: 3rem;
      text-align: center;
    }

    .hero h1 {
      font-size: 3rem;
    }

    .subtitle {
      margin: 0 auto;
    }

    .hero-form {
      max-width: 400px;
      margin: 0 auto;
    }

    .services-grid {
      grid-template-columns: 1fr;
    }

    .voice-card {
      flex-direction: column;
      text-align: center;
    }

    .voice-icon {
      margin: 0 auto;
    }

    .voice-features li {
      text-align: left;
    }
  }

  @media (max-width: 600px) {
    .hero {
      padding: 4rem 1.5rem 0;
    }

    .hero h1 {
      font-size: 2.25rem;
    }

    .intro, .services, .voice-section, .cta-section {
      padding: 4rem 1.5rem;
    }

    .intro h2, .cta-content h2 {
      font-size: 1.75rem;
    }

    .voice-card {
      padding: 2rem;
    }
  }
</style>
